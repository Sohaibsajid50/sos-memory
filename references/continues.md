# Continues

Supported command shapes for v1 documentation:

```bash
continues list
continues pick
continues claude 1
continues codex 1
continues gemini 1
continues antigravity 1
continues inspect <session-id> --preset full --write-md .continues-handoff.md
```

Implementation that depends on exact CLI output must be verified against the installed continues version before automation.
