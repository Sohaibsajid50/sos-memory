---
description: Refresh QMD collections for SOS memory
allowed-tools: Bash(node:*), Bash(qmd:*)
---

Run:

```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/sos.js" update-qmd
```

If QMD is missing, report the install guidance.
