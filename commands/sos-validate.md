---
description: Validate SOS memory installation and dependencies
allowed-tools: Bash(node:*), Bash(qmd:*), Bash(git:*)
---

Run:

```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/sos.js" validate
```

Summarize registry, QMD, continues, and version findings.
