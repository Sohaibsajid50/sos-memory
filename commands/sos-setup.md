---
description: Full guided SOS Memory onboarding — interview, provision, wire, verify
argument-hint: ""
---

Run the complete SOS Memory setup for this machine.

Invoke the `sos-setup` skill and follow it exactly: pre-flight platform
detection, the interview (use AskUserQuestion with labels AND descriptions for
every choice), config generation, provisioning, scheduled jobs + MCP wiring,
and `sos doctor` verification with the handoff card.
